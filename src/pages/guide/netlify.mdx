---
layout: ../../layouts/Base.astro
title: Netlify guide · filipino-talent.com
description: Deploy for free with Netlify and connect your filipino-talent.com subdomain.
---

# Netlify

## 1. Get your site online

1. Create a free Netlify account.
2. Drag your site folder onto the Netlify dashboard, or connect a Git repo.
3. Rename the site so it has a clean address: **Site configuration → Change
   site name** → `your-site`. Your URL becomes `https://your-site.netlify.app`.

## 2. Submit your request

On the [submit form](/submit):

- **Record type:** `CNAME`
- **Target:** `your-site.netlify.app`

## 3. Connect it at Netlify

Once your subdomain is live, tell Netlify to answer for it. That is the step
that actually makes the site load — follow the
[Netlify steps](/guide/connect#netlify).

## Notes

- Use the `netlify.app` address, not a deploy-preview URL.
- Keep DNS outside Netlify. Do not switch to Netlify DNS; it needs nameservers
  we cannot change.
